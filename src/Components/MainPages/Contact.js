import * as React from 'react';
import { Section, AboutTitle } from '../Reusable/Styles';

const Contact = () => (
	<Section id="contact">
		<AboutTitle>
			Contact
		</AboutTitle>
	</Section>
);

export default Contact;
